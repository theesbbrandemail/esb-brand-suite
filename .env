# REMOVED: Sensitive credentials were committed here and have been removed in favor of environment variables.
# Do NOT re-add real secrets to this file. Rotate any keys that were exposed.
# Example: store these values in Vercel Environment Variables or your deployment secrets manager.

# Supabase (client-safe)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase (server-only) - do NOT expose these to the browser
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_URL=
DATABASE_URL=

# Local development (optional)
#LOCAL_DEV=true
